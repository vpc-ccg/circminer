#ifndef __CHAIN_H__
#define __CHAIN_H__

#include <vector>
#include "common.h"

using namespace std;

void chain_seeds_n2(vector<fragment_t>& fragment_list, uint32_t fragment_count, chain_t &best_chain);

#endif
